var ssq = require('./lib/ssq.js');

ssq.players('tf2.rugc.eu', 27015, function(e, d) {
  if (e) {console.log(e); return;}
  if (d) {console.log('Good! ', d);
          d.forEach(function(player) {
            console.log(player.index, player.name, player.score, player.duration.toFixed(3))
          });
         }
});
